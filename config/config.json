{
  "application": {
    "serviceName": "$APPLICATION_NAME",
    "port": "$APP_PORT",
    "nodeEnv": "$NODE_ENV"
  },
  "db": {
    "username": "$MYSQL_DB_USERNAME",
    "password": "$MYSQL_DB_PASSWORD",
    "database": "$MYSQL_DB_NAME",
    "servers": "$MYSQL_DB_SERVERS",
    "dialect": "mysql",
    "logging": ""
  }
}
